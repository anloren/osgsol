# osgSol Earth 全产品 UI/UE 审计

日期：2026-07-24

状态：自动化整改完成，等待唯一桌面候选真机验收

## 审计目标

本审计把 osgSol Earth 的全部正式交互表面视为一套产品，而不是分别修补
ScienceEarth、AI、图层和状态面板。整改必须同时满足：

- 地球始终是主画布，工具不能无理由割裂或遮挡它；
- 相同语义在 ImGui 与 RmlUi 中使用同一套颜色、间距、边界和状态；
- 每个可见控件都必须能操作、能说明、能到达，装饰不能伪装成控件；
- 科学图表必须显示指标、单位、横纵坐标、时间范围、缺测和选中状态；
- 所有滚动区都能明确看见滚动位置，并可从两端往返；
- 不改动地球渲染、相机、地形、三维城市、数据算法、AI 工具协议和正常退出。

## 当前状态证据

用户提供的基线截图：

- 原始文件：`/Users/USER/Desktop/Screenshot 2026-07-24 at 11.21.07 AM.png`
- SHA-256：`5c5bb3c2f269bef56c820eb27c91cf20bcb88de15681e15246d6f1f21b2c60f0`
- 视口：2048 × 1152
- 路径：科学模块 → ERA5 Agricultural Climate → 锁定点位 →
  2017–2025 → 生成年度农业气候剖面 → 打开概览报告

当前证据只证明上述一个正式包状态。自动化不能启动或前置桌面 App，因此
后续视觉验收由离屏渲染证据与用户手动正式包验收共同完成。

## 全产品交互表面清单

| 区域 | 实现 | 主要任务 | 当前风险 |
|---|---|---|---|
| 顶部状态栏 | ImGui | 路径、坐标、高度、全局动作、系统状态 | 长文与右侧动作可能互相挤压 |
| 左侧模块轨 | ImGui | 探索、图层、科学、实时、卫星、三维、任务、设置 | 选中态与科学面板色彩不一致 |
| 模块抽屉 | ImGui/RmlUi | 模块参数与操作 | 两套主题、宽度和内边距体系 |
| 地图画布 | OSG | 地球导航和空间上下文 | UI 缝隙会露出无意义地图条带 |
| 科学工作台 | RmlUi | 方案、位置、时间、方法、执行 | 假滑条、边缘缝、固定宽度、按钮溢出 |
| 科学报告 | RmlUi | 概览、趋势、范围、证据 | 空白快照、无坐标图表、窗口边缘可达性 |
| AI 命令栏 | ImGui | 自然语言、模板、照片、视频 | 右侧按钮可能被裁剪，小屏缺少响应式换行 |
| 上下文托盘 | ImGui | 当前模块与选择对象摘要 | 占用整宽但信息密度低 |
| 实体详情卡 | ImGui | 航班、卫星、地点等对象信息 | 浮窗层级与关闭动作不统一 |
| 弹窗/确认 | ImGui/RmlUi | 更多操作、删除、视频确认 | 两套尺寸、危险色和键盘行为 |
| 旧科学回退 | ImGui | 调试/回退 | 不应与正式 RmlUi 同时争夺输入 |

## 发现矩阵

| 编号 | 严重度 | 发现 | 结构原因 | 验收方式 |
|---|---|---|---|---|
| UI-001 | P0 | 科学报告概览是一整块浅色空白 | 快照在最终地图合成前 PRE_DRAW 读取 | 离屏保存快照必须含非均匀地图像素 |
| UI-002 | P0 | 左侧面板与滚动条旁出现透明细缝 | Shell `outerGap` 被用于已停靠抽屉 | 导航轨与抽屉边界连续，无地图漏缝 |
| UI-003 | P1 | 年份下方条带看似滑杆却不能操作 | `.range-strip` 是纯装饰 | 删除伪控件，改为明确的年度摘要 |
| UI-004 | P0 | 趋势曲线没有坐标与单位 | 自定义图表只画线和网格 | 显示标题、单位、X/Y 刻度、年份和缺测说明 |
| UI-005 | P0 | 部分按钮被面板/视口遮挡 | 固定宽度与硬编码动作区 | 1024/1440/2048 三档边界测试无裁剪 |
| UI-006 | P1 | 科学面板与主 UI 色彩不匹配 | RmlUi 与 ImGui 各自硬编码 token | 跨引擎 token 合同测试 |
| UI-007 | P1 | 面板卡片在宽度变化时溢出 | 子元素固定 290/320/360 px | 340–380 px 工作台运行时无横向溢出 |
| UI-008 | P1 | 滚动条与内容区像两块拼接 | `scrollbar-margin` 和独立背景轨 | 滚动条贴合容器内部边缘、两端可回滚 |
| UI-009 | P1 | AI 右侧动作可能被裁剪 | 固定动作宽度、标签长度未计入 | 动作行按可用宽度布局或换行 |
| UI-010 | P1 | 科学报告与主壳层层级语言割裂 | 圆角、边框、背景、强调色不一致 | 统一窗口、卡片、标签、按钮规范 |
| UI-011 | P2 | 永久解释占据操作空间 | 说明与主要任务未分层 | 主流程只留必要短文，详细解释按需展开 |
| UI-012 | P1 | 现有测试偏向源字符串 | 缺少真实布局和像素证据 | 扩展离屏 RmlUi/GL 几何与截图测试 |
| UI-013 | P0 | 非科学详情内容变长后，洞察透镜可越过预留边界且没有有效滚动范围 | `AlwaysAutoResize` 与高度约束组合不能形成稳定滚动窗口 | 使用上一帧实测内容高度，窗口始终钳制在洞察轨道内，长内容产生真实纵向滚动 |
| UI-014 | P1 | 书签、巡游、新闻详情的同行动作在翻译或紧凑宽度下仍可能被裁剪 | 局部遗留无条件 `SameLine` | 每个紧凑动作按实际文本宽度判断同行或换行 |
| UI-015 | P1 | 模块帮助和旧科学回退帮助仍可能按内容无限扩张 | 自动尺寸弹窗没有视口上限 | 帮助弹窗统一使用视口约束和容器内换行 |
| UI-016 | P2 | ImGui 科学图表和 AI 图表复制近似 RGB 常量 | 自绘图表绕过共享 token | 自绘颜色由共享 token 转换，只允许覆盖透明度 |

## 整改结果

截至 2026-07-24，UI-001 至 UI-016 的自动化整改均已进入生产实现：

- 最终地图合成后、UI 绘制前采集科学报告概览；单色白/黑错误帧会被拒绝并重试；
- 模块轨、抽屉和滚动条连续停靠，删除透明地图细缝；
- 删除无交互能力的年份伪滑杆，保留可读年份字段与范围摘要；
- 科学趋势图补齐指标、单位、聚合、X/Y 刻度、缺测、选中年和统计摘要；
- ImGui 与 RmlUi 共享同一套颜色、层级、边界和状态语法；
- 工作台、报告、AI 命令栏、顶部动作和对象卡片均按可用空间布局；
- 探索、图层、实时、卫星、三维、任务、设置和状态区迁入统一组件；
- 洞察透镜改为实测高度、轨道内钳制和真实纵向滚动；自动检查可以下滚并返回；
- 紧凑按钮行、帮助弹窗和自绘图表颜色也进入共享布局与 token 契约；
- 五个科学数据源切换、真实菜单、滚轮往返、分析提交和报告操作均由运行时测试覆盖；
- 1024×576、1440×900、2048×1152 三档壳层边界已自动验证。

完整离线回归为 80/80 通过。自动化不会把离屏证据冒充桌面真机观感；
仍须由用户检查真实 Retina 显示、地图与科学图层融合手感，以及正常 Quit 后
macOS 不产生新的异常退出报告。

## 结构问题与表面问题

### 必须先解决的结构问题

1. 建立跨 ImGui/RmlUi 的唯一设计 token。
2. 停靠区域与浮动区域使用不同的布局规则；停靠区不留透明外缝。
3. 工作台、报告、命令栏全部采用可用宽度计算，不依赖固定子元素宽度。
4. 科学快照改为最终地图合成后、任何 UI 绘制前采集。
5. 图表建立完整的数据可视化语法，而不是折线装饰。
6. 自动化覆盖 1024×576、1440×900、2048×1152 和超长中英文内容。

### 随后处理的视觉润色

- 字重、行高、圆角和卡片密度；
- hover、focus、pressed、disabled、busy、error 状态；
- 图例、警示和空状态文案；
- 窗口移动、最小化与恢复的动效节奏；
- 长列表、详情卡与图表导出布局。

## 可访问性与可操作性风险

- 纯颜色不能承担唯一状态表达；必须同时有文字、形状或图标。
- 所有焦点态需要明显边界，不能只用极浅色差。
- 最小点击目标 32 px，主操作和模块项不小于 40 px。
- 键盘顺序跟随任务顺序；Escape 只关闭当前层，不退出 App。
- 禁用态必须说明原因；装饰条、标签和标题不能使用按钮外观。
- 科学数值不使用难读的无单位科学计数法作为默认表达。

## 验收门槛

每个模块必须完成以下同一套检查：

1. 空、加载、成功、部分、失败、禁用和长文本状态；
2. 1024×576、1440×900、2048×1152 边界与遮挡；
3. 鼠标、滚轮、键盘和文本输入；
4. 打开、关闭、最小化、恢复、切换模块；
5. 地图仍可见、可导航、不会被透明缝或永久大段说明破坏；
6. 自动化通过后才进入唯一桌面包的用户手动验收。
