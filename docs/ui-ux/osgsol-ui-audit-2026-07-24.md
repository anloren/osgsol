# osgSol Earth 全产品 UI/UE 审计

日期：2026-07-24

状态：自动化整改完成，等待唯一桌面候选真机验收

## 审计目标

本审计把 osgSol Earth 的全部正式交互表面视为一套产品，而不是分别修补
ScienceEarth、AI、图层和状态面板。整改必须同时满足：

- 地球始终是主画布，工具不能无理由割裂或遮挡它；
- 相同语义在 ImGui 与 RmlUi 中使用同一套颜色、间距、边界和状态；
- 每个可见控件都必须能操作、能说明、能到达，装饰不能伪装成控件；
- 科学图表必须显示指标、单位、横纵坐标、时间范围、缺测和选中状态；
- 所有滚动区都能明确看见滚动位置，并可从两端往返；
- 不改动地球渲染、相机、地形、三维城市、数据算法、AI 工具协议和正常退出。

## 当前状态证据

用户提供的基线截图：

- 原始文件：`/Users/USER/Desktop/Screenshot 2026-07-24 at 11.21.07 AM.png`
- SHA-256：`5c5bb3c2f269bef56c820eb27c91cf20bcb88de15681e15246d6f1f21b2c60f0`
- 视口：2048 × 1152
- 路径：科学模块 → ERA5 Agricultural Climate → 锁定点位 →
  2017–2025 → 生成年度农业气候剖面 → 打开概览报告

当前证据只证明上述一个正式包状态。自动化不能启动或前置桌面 App，因此
后续视觉验收由离屏渲染证据与用户手动正式包验收共同完成。

## 全产品交互表面清单

| 区域 | 实现 | 主要任务 | 当前风险 |
|---|---|---|---|
| 顶部状态栏 | ImGui | 路径、坐标、高度、全局动作、系统状态 | 长文与右侧动作可能互相挤压 |
| 左侧模块轨 | ImGui | 探索、图层、科学、实时、卫星、三维、任务、设置 | 选中态与科学面板色彩不一致 |
| 模块抽屉 | ImGui/RmlUi | 模块参数与操作 | 两套主题、宽度和内边距体系 |
| 地图画布 | OSG | 地球导航和空间上下文 | UI 缝隙会露出无意义地图条带 |
| 科学工作台 | RmlUi | 方案、位置、时间、方法、执行 | 假滑条、边缘缝、固定宽度、按钮溢出 |
| 科学报告 | RmlUi | 概览、趋势、范围、证据 | 空白快照、无坐标图表、窗口边缘可达性 |
| AI 命令栏 | ImGui | 自然语言、模板、照片、视频 | 右侧按钮可能被裁剪，小屏缺少响应式换行 |
| 上下文托盘 | ImGui | 当前模块与选择对象摘要 | 占用整宽但信息密度低 |
| 实体详情卡 | ImGui | 航班、卫星、地点等对象信息 | 浮窗层级与关闭动作不统一 |
| 弹窗/确认 | ImGui/RmlUi | 更多操作、删除、视频确认 | 两套尺寸、危险色和键盘行为 |
| 旧科学回退 | ImGui | 调试/回退 | 不应与正式 RmlUi 同时争夺输入 |

## 发现矩阵

| 编号 | 严重度 | 发现 | 结构原因 | 验收方式 |
|---|---|---|---|---|
| UI-001 | P0 | 科学报告概览是一整块浅色空白 | 快照在最终地图合成前 PRE_DRAW 读取 | 离屏保存快照必须含非均匀地图像素 |
| UI-002 | P0 | 左侧面板与滚动条旁出现透明细缝 | Shell `outerGap` 被用于已停靠抽屉 | 导航轨与抽屉边界连续，无地图漏缝 |
| UI-003 | P1 | 年份下方条带看似滑杆却不能操作 | `.range-strip` 是纯装饰 | 删除伪控件，改为明确的年度摘要 |
| UI-004 | P0 | 趋势曲线没有坐标与单位 | 自定义图表只画线和网格 | 显示标题、单位、X/Y 刻度、年份和缺测说明 |
| UI-005 | P0 | 部分按钮被面板/视口遮挡 | 固定宽度与硬编码动作区 | 1024/1440/2048 三档边界测试无裁剪 |
| UI-006 | P1 | 科学面板与主 UI 色彩不匹配 | RmlUi 与 ImGui 各自硬编码 token | 跨引擎 token 合同测试 |
| UI-007 | P1 | 面板卡片在宽度变化时溢出 | 子元素固定 290/320/360 px | 340–380 px 工作台运行时无横向溢出 |
| UI-008 | P1 | 滚动条与内容区像两块拼接 | `scrollbar-margin` 和独立背景轨 | 滚动条贴合容器内部边缘、两端可回滚 |
| UI-009 | P1 | AI 右侧动作可能被裁剪 | 固定动作宽度、标签长度未计入 | 动作行按可用宽度布局或换行 |
| UI-010 | P1 | 科学报告与主壳层层级语言割裂 | 圆角、边框、背景、强调色不一致 | 统一窗口、卡片、标签、按钮规范 |
| UI-011 | P2 | 永久解释占据操作空间 | 说明与主要任务未分层 | 主流程只留必要短文，详细解释按需展开 |
| UI-012 | P1 | 现有测试偏向源字符串 | 缺少真实布局和像素证据 | 扩展离屏 RmlUi/GL 几何与截图测试 |
| UI-013 | P0 | 非科学详情内容变长后，洞察透镜可越过预留边界且没有有效滚动范围 | `AlwaysAutoResize` 与高度约束组合不能形成稳定滚动窗口 | 使用上一帧实测内容高度，窗口始终钳制在洞察轨道内，长内容产生真实纵向滚动 |
| UI-014 | P1 | 书签、巡游、新闻详情的同行动作在翻译或紧凑宽度下仍可能被裁剪 | 局部遗留无条件 `SameLine` | 每个紧凑动作按实际文本宽度判断同行或换行 |
| UI-015 | P1 | 模块帮助和旧科学回退帮助仍可能按内容无限扩张 | 自动尺寸弹窗没有视口上限 | 帮助弹窗统一使用视口约束和容器内换行 |
| UI-016 | P2 | ImGui 科学图表和 AI 图表复制近似 RGB 常量 | 自绘图表绕过共享 token | 自绘颜色由共享 token 转换，只允许覆盖透明度 |
| UI-017 | P0 | 展开左侧抽屉后，AI 命令栏仍按“无抽屉”地图宽度布局并压在面板下方 | AI 子系统自行重算并硬编码 `drawerOpen=true` 的 Shell，命令区没有使用当前帧实际布局 | 命令栏消费唯一 Shell；1024/1440/2048 三档均位于左面板与洞察透镜之间 |
| UI-018 | P0 | 再次点击“科学”后状态显示已收起，但 RmlUi 科学工作台仍留在屏幕上 | 科学产品表面的显隐只检查模块与运行时就绪，没有检查 `drawerOpen` | 同一模块开/关切换必须同步隐藏/恢复科学工作台 |
| UI-019 | P1 | 科学工作台展开时，地图细节提示会压住其右缘约 30 px | 提示条只预留 310 px 原生抽屉，未预留 340 px 科学工作台 | 地图提示使用与命令栏相同的左面板安全边界 |
| UI-020 | P1 | 禁用说明、缺测提示等小字号弱文本在抬升表面上对比度不足 | 旧 `text-muted` 在最浅常用表面上只有约 4.05:1 | 将共享弱文本 token 调整为 `#7F8684`，自动门禁要求正常字号文本在最浅常用表面达到至少 4.5:1 |
| UI-021 | P1 | 原生 ImGui 表面没有产品级 Tab 导航保证 | 键盘导航未在统一上下文启动时开启 | 全局启用键盘导航，并用真实两个字段证明 Tab 推进焦点 |
| UI-022 | P1 | 短面板也永久显示滚动条，形成“有内容可滚”的错误暗示 | 抽屉、AI 历史和旧科学结果使用 `AlwaysVerticalScrollbar` | 短内容不显示装饰滚动条，长内容出现真实滚动范围并可下滚、返回 |
| UI-023 | P1 | 图层预设、事件流和状态带混入不相关模块，切换后仍像上一模块 | 控制项按共享图层目录绘制，没有严格归属和空状态 | 图层预设只属于图层，事件流/状态带只属于任务，各模块显示明确空/筛选无结果状态 |
| UI-024 | P2 | 中英文在同一控件中机械重复，造成面板拥挤和阅读噪声 | 历史功能逐项追加双语标签 | 面向当前产品语言使用简明中文，保留必要官方数据名、缩写和单位 |
| UI-025 | P0 | 改变窗口尺寸后科学工作台仍保留旧 720 px 高度并越出视口 | Resize 没有传给 RmlUi；运行时只改 `ElementDocument`，未覆盖固定 RCSS body | 真实 Resize 更新上下文/渲染器，生产 presenter 同步 body；1024×576→1440×900 往返无越界 |
| UI-026 | P1 | 八模块测试直接写状态，不能证明屏幕上的按钮真能点 | 逻辑测试绕过鼠标命中与 ImGui 输入路径 | 对八个可见模块按钮逐一发送鼠标移动、按下和释放，并验证选择与再次点击收起 |
| UI-027 | P2 | Retina 清晰度不能靠 plist 单开而不破坏坐标 | OSG 3.6.5 Cocoa 后端明确关闭 high-resolution OpenGL surface；现有地图、拾取、ImGui、RmlUi 和输入法按 1 点=1 像素协作 | 本版本保持稳定 1:1 契约；未来升级必须同时改 backing-size viewport、渲染与全部输入坐标，不能作为局部 UI 开关 |
| UI-028 | P0 | “分析方法”旁的问号看似可点但没有任何响应 | RML 中存在无 ID、无事件绑定的可见按钮 | 所有可见 RmlUi 按钮必须有稳定 ID、presenter 绑定；真实点击打开说明 |
| UI-029 | P0 | 先操作左侧工作台，再打开报告时，报告标签和窗口按钮可能显示但点不到 | RmlUi 点击工作台后把 composer 文档提升到报告之上；报告打开时没有重新取得文档前景层 | 可见报告必须 `PullToFront()`；真实点击四个标签并验证内容逐项切换 |
| UI-030 | P1 | 原生壳层只有几何/输入断言，仍可能在真实 GL 中出现白底、透明缝或整块覆盖地图 | 既有测试没有读取原生 ImGui 最终像素 | 使用生产字体和组件离屏渲染八模块壳层；断言连续深色表面、地图保留和无大块浅色回退 |
| UI-031 | P1 | 900/1080 高度下上下文托盘的中文标签和值可能被底边裁剪 | 固定 46–58 px 高度不足以容纳两行字体、内边距和边框 | 托盘高度调整为 58–64 px，真实 GL 像素和无滚动范围断言共同覆盖 |
| UI-032 | P1 | 洞察透镜首次呈现标签时可能只剩标题，正文下一帧被裁掉 | `CardStack` 在尚无激活标签正文的瞬时帧错误记录首选高度 | 只有真正绘制标签正文后才更新实测高度；两帧真实 GL 渲染必须保留完整内容 |
| UI-033 | P0 | 1024×576 下 AI 动作换到第二行后仍按单行高度预留，命令栏会压住抽屉 | Shell 在知道真实命令宽度前先固定 72 px 高度 | 先算可用宽度；窄屏为双行动作保留 104–118 px，真实产品入口证明不与抽屉、托盘、洞察透镜重叠 |
| UI-034 | P1 | 左侧模块轨存在肉眼不易发现的横向溢出 | 按钮固定减 18 px，却没有扣除主题左右共 24 px 内边距 | 按钮使用真实内容宽度；生产窗口禁止横向滚动条和超额水平范围 |
| UI-035 | P1 | 正常宽度 AI 输入与动作合计仍多出一个 8 px 间距，最右按钮可能贴边或被裁 | 行布局只减动作宽度，漏减输入与首个动作之间的 `ItemSpacing` | 输入宽度同时扣除动作组和实际间距；1440/2048 生产入口无水平溢出 |
| UI-036 | P0 | 代表性壳层测试通过，但真实 `EarthControlUI` 入口仍可能出现重叠、假滚动和业务长文裁剪 | 旧像素测试重建了组件外观，没有执行全部生产绘制入口 | 新增真实八模块 × 三视口 × 长文/状态/实体卡的 24 画面 GL 门禁，并真实滚到底再返回顶部 |
| UI-037 | P0 | ImGui 主壳层与 RmlUi 科学界面分别通过时，仍不能证明同一帧合成后互不遮挡 | 两套渲染后端原先由独立测试夹具验证，缺少真实绘制顺序下的共享安全区门禁 | 在同一个 CGL 帧中先绘制 RmlUi 科学工作台/报告、再合成 ImGui Shell；1024×576、1280×720、1440×900 验证工作台边界，1440×900 继续验证报告与关键控件 |
| UI-038 | P0 | 科学报告与正式 AI 命令栏在 1440×900 实际重叠约 33 px，但轻量命令栏替身没有暴露该问题 | AI 默认态把标题、历史、分析模板和输入无条件拆成多行，真实窗口高度超过 Shell 预留；组合测试没有执行正式 `AIChatUI` | 组合测试直接调用 `AIChatUI::draw` 并比较实际窗口矩形；宽屏工具头同排、窄屏规则换行，报告底边必须位于真实 AI 窗口顶边之上 |
| UI-039 | P0 | 完成矩阵声称报告窗口操作已覆盖，但测试只真实点击了四个标签 | 模型测试与事件绑定被错误等同于真实指针链路 | 真实点击指标、复制编号、更多、删除取消/确认、最小化/恢复、关闭和返回区域，并核对唯一动作队列 |
| UI-040 | P0 | 科学位置卡的两个并排按钮都可见，但第一个命中区覆盖第二个约 127 px，导致后一个点不动 | RmlUi 对该布局中的 `flex: 1` 计算产生重叠盒；旧测试只量视觉宽高，没有核对命中对象 | 改为一个明确的原生位置操作菜单；真实选择地图中心、当前视野、更新和返回，禁止重复动作 |
| UI-041 | P1 | AI 命令栏的“照片”“视频”使用 40 px 固定宽度，中文标签在真实 CJK 字体下被裁 | Shell 预留动作宽度与生产按钮宽度都小于真实文本和内边距 | 统一按 52 px 预留并量取每个按钮真实矩形；历史展开态像素证据不得裁字或越界 |
| UI-042 | P1 | 分析模板浮层向下覆盖 AI 命令栏顶部，虽然二者都在视口内 | 弹层只有尺寸约束，没有使用当前 Shell 的命令栏安全边界 | 弹层固定出现在命令栏上方；真实鼠标打开/外点关闭，并断言两个活动窗口矩形不重叠 |
| UI-043 | P1 | AI 柱状图的指标名和数值在窄卡片内互相挤压，长值会被固定列截断 | 图表把名称和值硬分配到 36%/46 px 固定列 | 名称允许换行，数值按真实宽度右对齐，数据条独占下一行；真实 GL 验证长名称、四位数值和完整条形 |
| UI-044 | P1 | 照片、视频和长任务标题只验证了逻辑状态，仍可能出现动作不明确、标签裁剪或 Tab 被长标题撑破 | 媒体结果与生成进度没有进入真实生产卡片像素门禁 | 使用固定“生成任务”Tab、正文展示完整任务名；照片/视频使用全宽明确动作，生成进度显示状态和百分比 |
| UI-045 | P1 | 底部状态带在较矮视口中可能超出 Shell 安全区 | ImGui 默认 32 px 最小窗口高度覆盖了产品计算的 24–30 px 状态带 | 状态带暂时解除默认最小高度并使用 2 px 垂直内边距；真实 GL 比较状态带与上下文托盘边界 |
| UI-046 | P1 | 最高细节提示虽应只出现一次，但缺少真实关闭链路，可能成为无法消失的永久遮挡 | 原测试只检查状态模型，没有点击正式提示关闭按钮 | 真实 GL 只允许一个受限提示，按实际按钮中心发送点击，并在下一帧验证完全消失且不压抽屉/洞察区 |
| UI-047 | P1 | 模块帮助可能漂到地图中央，与触发控件和所属面板脱节 | 帮助弹层默认使用最后鼠标位置，不使用触发表面锚点 | 弹层锚定模块抽屉/触发控件、最大宽度 300 px、自动换行并钳制在抽屉内；外点关闭 |
| UI-048 | P0 | AI 图表/媒体/进度、事件流/状态带、最高细节提示和模块帮助均属于正式可见表面，但完成矩阵缺少 A 级像素证据 | 生产 UI 测试偏向主流程，隐藏和条件表面仅有替身或合同检查 | 直接链接正式生产实现并为每个条件表面保存、检查真实离屏 GL 证据 |
| UI-049 | P0 | 趋势内容已经显示时，旧 PNG 证据仍把“概览”画成选中态 | 组合夹具只切换内容，没有同步 Tab class；测试只写新 PPM，人工转换的 PNG 可长期停留在旧帧 | 内容与活动 Tab 必须同时断言；每次 GL 测试在同一次写入中生成同名 PPM 与 PNG，禁止复用旧转换图 |
| UI-050 | P0 | 抽屉截图存在不能证明内部控件真的可点击，完成矩阵仍可能把“画出来了”误记为“功能已验证” | 正式 `EarthControlUI` 没有测试期命中矩形，既有 GL 门禁只操作滚动和少量条件表面 | 对顶栏、探索、跳转、书签、图层、任务、设置和 Quit 使用正式命中区发送鼠标按下/释放，并核对相机、操纵器、`LayerManager`、配置注册表和 `QuitRequest` 的真实状态变化 |
| UI-051 | P0 | AI 输入、模板、照片和视频按钮即使完整显示，也不能证明提交了正确命令，尤其可能再次把旧位置或旧图带入新请求 | 既有门禁只检查命令栏矩形和弹窗，没有截获正式按钮产生的请求 | 输入并发送精确文本；模板真实定位并提交精确提示词；照片必须提交“当前视角”；视频起点、终点、行内取消、确认和弹窗取消逐项点击并断言请求类型 |
| UI-052 | P1 | 手动太阳日期和跳转输入只被记录为“可达”，测试仍可能直接写内部变量后点击按钮 | 输入框没有真实键盘编辑证据，字段可见与字段可用被混为一谈 | 通过正式输入框执行全选、键盘输入与提交；核对年月日字段、跳转经纬度/高度以及最终相机状态 |
| UI-053 | P0 | 剩余少数正式控件仍只有“绑定存在”或程序直接改状态的间接证据 | 结束年份、运行中取消、第二/第三结果架槽、模块帮助、事件定位和洞察关闭没有走真实命中链路 | 对每个条件控件发送真实指针/键盘事件，核对年份文本、取消事件、结果 ID、弹窗、相机位置和关闭回调 |

## 整改结果

截至 2026-07-24，UI-001 至 UI-050 的生产整改和 UI-051 至 UI-052 的可操作性
证据整改均已完成；UI-027 作为渲染器级现代化事项被明确隔离，没有用高风险
plist 开关冒充修复：

- 最终地图合成后、UI 绘制前采集科学报告概览；单色白/黑错误帧会被拒绝并重试；
- 模块轨、抽屉和滚动条连续停靠，删除透明地图细缝；
- 删除无交互能力的年份伪滑杆，保留可读年份字段与范围摘要；
- 科学趋势图补齐指标、单位、聚合、X/Y 刻度、缺测、选中年和统计摘要；
- ImGui 与 RmlUi 共享同一套颜色、层级、边界和状态语法；
- 工作台、报告、AI 命令栏、顶部动作和对象卡片均按可用空间布局；
- 探索、图层、实时、卫星、三维、任务、设置和状态区迁入统一组件；
- 洞察透镜改为实测高度、轨道内钳制和真实纵向滚动；自动检查可以下滚并返回；
- 紧凑按钮行、帮助弹窗和自绘图表颜色也进入共享布局与 token 契约；
- AI 命令栏、地图提示和科学工作台现在消费同一帧 Shell 状态，不再互相覆盖；
- 科学模块的收起/展开状态同时控制 RmlUi 产品表面，不再出现“状态已收起、面板仍在”的分裂；
- 弱文本 token 在 ImGui 与 RmlUi 同步提升，并由 4.5:1 对比度门禁阻止回退；
- Escape、Return 与 Tab 均必须向 RmlUi 发送成对键盘事件；模块—图层组匹配由运行时测试隔离；
- 五个科学数据源切换、真实菜单、滚轮往返、分析提交和报告操作均由运行时测试覆盖；
- 原生 ImGui 启用产品级键盘导航，真实 Tab 焦点推进由运行时门禁覆盖；
- 短内容不再显示装饰滚动条；原生抽屉、AI 和科学回退只在真实溢出时显示滚动；
- 图层预设、事件流、状态带与数据源按模块归属分离，空状态与筛选无结果不再混淆；
- 可见的八个主模块按钮全部经过真实鼠标命中、按下、释放、切换与收起测试；
- 窗口 Resize 现在同步 RmlUi 上下文、渲染器和生产科学工作台 body；
  1024×576 与 1440×900 可往返重排；
- 所有可见 RmlUi 按钮均受稳定 ID/绑定门禁保护，“分析方法”帮助已成为真实说明开关；
- 科学报告打开时主动取得 RmlUi 前景层，四个报告标签由真实指针点击逐项验证；
- 报告快照不可用时显示深色、可解释空态，不再用整块白色区域冒充分析范围；
- 原生壳层以生产 CJK 字体和组件完成八模块真实 OpenGL 离屏像素门禁；
- 上下文托盘提高到能完整容纳两行信息的 58–64 px，并禁止产生伪滚动范围；
- 洞察透镜不再用“尚无激活标签”的瞬时帧高度覆盖正式正文高度；
- 窄屏 AI 动作换行后，Shell 同步保留双行高度，不再压住抽屉、托盘或洞察透镜；
- 模块轨按钮与 AI 输入行按真实内容宽度和间距计算，禁止隐藏的横向溢出；
- 新门禁直接调用正式 `EarthControlUI::runInternal` 与 `AIChatUI::draw`，
  覆盖业务长文、加载/失败状态和航班、卫星、船舶、事件详情；
- 探索与图层的真实长抽屉在三档视口均执行滚到底和返回顶部；
- 1024×576、1440×900、2048×1152 三档壳层边界已自动验证。
- 新增跨引擎同帧门禁：RmlUi 工作台/报告与 ImGui Shell 在同一 CGL 帧、
  按正式层次顺序合成；1024×576、1280×720、1440×900 均检查左侧连续停靠
  和 AI 安全区，1440×900 继续检查报告的顶部/左侧/底部边界，并真实点击
  数据源菜单、年份输入、滚轮双向、开始分析和带坐标轴的趋势图。
- 跨引擎门禁已去除轻量 AI 栏替身，直接执行正式 `AIChatUI::draw`。
  新门禁首先复现报告底边 `708.98`、AI 顶边 `676.00` 的真实重叠，再通过
  响应式工具头压缩正式 AI 默认态；修复后用实际 ImGui 窗口矩形验证三档
  工作台/AI 边界及 1440×900 报告/AI 边界。
- 报告窗口的指标切换、复制编号、更多、删除取消/确认、最小化/恢复、关闭和
  返回分析区域全部纳入真实 RmlUi 点击链路，不再用模型测试冒充可操作性。
- 科学位置操作由两个会重叠命中的并排按钮收敛为单一原生菜单；地图中心、
  当前视野、更新和返回四条正式动作均验证事件名称、数量和目标状态。
- AI 历史、分析模板和视频确认进入正式 `AIChatUI` 鼠标/像素门禁；照片与
  视频动作按真实 CJK 标签宽度预留，模板浮层被约束在命令栏上方。
- AI 柱状图取消固定名称/数值列，名称、数值和条形按内容分层；折线、柱状、
  环图和统计卡四种正式图表均生成真实 GL 证据。
- AI 照片、视频和生成进度使用明确的文件标签、全宽主动作和百分比；超长任务
  标题不再撑破 Tab，而是在正文中完整换行。
- 事件流、底部状态带、最高细节单次提示和模块帮助均进入生产入口的鼠标、
  滚动、关闭、边界和像素门禁；帮助弹层锚定所属抽屉，不再漂到地图中央。
- 科学报告四个标签的内容可见性与 `selected` class 逐项绑定；组合图同步显示
  “时间趋势”选中态。所有 UI GL 门禁在同一次运行中同时写入 PPM 和 PNG，
  文档与人工复核不再读取历史转换图。
- 正式 ImGui 控件不再以“模块截图存在”冒充可操作性。测试期命中矩形覆盖顶栏、
  常昼/真实太阳、海洋/曝光、相机跳转、书签记录/播放/停止/清空、图层预设与
  开关、任务状态、设置恢复默认及 Quit；每次点击都核对对应生产状态。
- 手动太阳年月日与跳转经纬度/高度使用正式输入框接受键盘编辑；太阳方向、UTC、
  手动曝光、大气强度、全部已注册场景预设、所属模块图层和每个设置参数都真实
  改变业务状态，不再由测试直接写值冒充交互。
- AI 输入发送、模板定位与精确提示词、当前视角照片请求、视频起点/终点、行内
  取消、确认和弹窗取消均点击正式控件并核对准确请求；审计构建只截获外部副作用，
  不联网、不截图、不生成文件，生产构建不包含测试状态。
- 科学 ImGui 旧面板继续只作为 RmlUi/插件失败时的回退；空服务、空预览层和空
  操纵器均安全返回，不会在启动或退出的部分初始化状态中解引用。
- 完成性复核不再把监听器绑定或测试直接写状态当作真实交互：起止年份分别接受
  指针与键盘，运行中取消收到正式点击；三个结果架槽逐项恢复正确 artifact；
  抽屉和上下文帮助均由正式问号打开；事件项点击真实定位地球，洞察标签关闭
  真实触发生产回调。
- 新增 `docs/ui-ux/osgsol-ui-surface-inventory.md`，明确正式主路径、条件表面、
  科学故障回退、不可达历史 UI、外部副作用边界与唯一 M 级验收门槛。

当前完成性复核提交 `46926af` 的完整生产目标构建成功，离线回归为 82/82 通过，
包审计单元测试 77/77 通过。事件命中矩形仍只存在于
`OSGSOL_UI_AUDIT_HOOKS` 测试构建；正常生产构建不包含事件审计字段和外部副作用
截获路径。自动化不会把离屏证据冒充桌面真机观感；
仍须由用户检查真实 Retina 显示、地图与科学图层融合手感，以及正常 Quit 后
macOS 不产生新的异常退出报告。

本轮残余审计的离屏 GL 证据重新生成并人工检查：数据源菜单、运行中页脚和趋势
报告均使用当前生产 Rml/CSS；报告已呈现完整坐标、单位和统计摘要。正式候选的
G0-v2 包审计为 PASS，新增发现 0、未解析依赖 0、违规 0，四项体积门槛全部
PASS。审计仍如实列出一个已受 reference/ratchet 约束的历史编译路径根因：
`Contents/lib/libosgDB.3.6.5.dylib` 中的
`${SOURCE_ROOT}/build/sdk_core/lib/osgPlugins-3.6.5`；本次没有新增或扩大。
八个原生模块壳层证据位于
`build/science_g3_release/ui-evidence/imgui-shell/earth-ui-*.png`；
直接执行生产入口的八模块 × 三视口证据位于
`build/science_g3_release/ui-evidence/product-shell/*.png`；
AI 图表、媒体、生成进度、事件流/状态带、最高细节提示和模块帮助的条件表面
证据位于 `build/science_g3_release/ui-evidence/product-shell/`；
科学工作台与报告的同帧组合证据位于
`build/science_g3_release/ui-evidence/science-composite/`：
`workbench-shell-1024x576.png`、`workbench-shell-1280x720.png`、
`workbench-shell.png`、`menu-open.png`、`running.png`、`overview.png`
和 `trend-report.png`。这些图均由无前台 CGL 离屏运行时生成。
桌面包与严格暂存包的 371 个普通文件和 37 个符号链接完全一致；自动化没有启动
桌面 App。当前候选来源为 `9f8f64a0517d22737cbfbdc70f3feb66f24ce45e`，
可执行文件 SHA-256 为
`21f47ca70dbd76e00e0859e04d03fb11c2d77d84b5f0512cc6be81c8260ab739`，
桌面与严格暂存包的整包内容与模式指纹同为
`bb34ad6d9d47e4e37b190ea90b2a1729aba855a3a0b3f80c058ebf5f52ba58c3`。

## 结构问题与表面问题

### 必须先解决的结构问题

1. 建立跨 ImGui/RmlUi 的唯一设计 token。
2. 停靠区域与浮动区域使用不同的布局规则；停靠区不留透明外缝。
3. 工作台、报告、命令栏全部采用可用宽度计算，不依赖固定子元素宽度。
4. 科学快照改为最终地图合成后、任何 UI 绘制前采集。
5. 图表建立完整的数据可视化语法，而不是折线装饰。
6. 自动化覆盖 1024×576、1440×900、2048×1152 和超长中英文内容。

### 随后处理的视觉润色

- 字重、行高、圆角和卡片密度；
- hover、focus、pressed、disabled、busy、error 状态；
- 图例、警示和空状态文案；
- 窗口移动、最小化与恢复的动效节奏；
- 长列表、详情卡与图表导出布局。

## 可访问性与可操作性风险

- 纯颜色不能承担唯一状态表达；必须同时有文字、形状或图标。
- 所有焦点态需要明显边界，不能只用极浅色差。
- 最小点击目标 32 px，主操作和模块项不小于 40 px。
- 键盘顺序跟随任务顺序；Escape 只关闭当前层，不退出 App。
- 禁用态必须说明原因；装饰条、标签和标题不能使用按钮外观。
- 科学数值不使用难读的无单位科学计数法作为默认表达。

## 验收门槛

每个模块必须完成以下同一套检查：

1. 空、加载、成功、部分、失败、禁用和长文本状态；
2. 1024×576、1440×900、2048×1152 边界与遮挡；
3. 鼠标、滚轮、键盘和文本输入；
4. 打开、关闭、最小化、恢复、切换模块；
5. 地图仍可见、可导航、不会被透明缝或永久大段说明破坏；
6. 自动化通过后才进入唯一桌面包的用户手动验收。

## 正式表面边界

`applications/earth_explorer/ui.cpp` 中的 Drawer2D 城市演示 UI 仍因历史构建关系参与
编译，但唯一调用位于 `#if !SIMPLE_VERSION`，正式 Earth Explorer 固定
`SIMPLE_VERSION=1`，因此该表面不会进入运行时。本审计不把这段死代码算作已迁移
产品 UI，也不允许它作为正式候选的视觉证据。正式表面以
`EarthControlUI`、`earth_ui_v2`、RmlUi ScienceEarth、AI Command、
Insight Lens、状态栏和受约束弹窗为准。
